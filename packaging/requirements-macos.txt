# Experimental macOS environment; resolve/build on the target architecture.
# Python 3.12 recommended for this first port. Native validation still needed.
-r requirements-studio.txt
# 1.24.4 no longer supplies an Intel Mac wheel; 1.23.2 has both architectures.
onnxruntime==1.23.2; sys_platform == 'darwin'
# 50.x no longer supplies an Intel Mac wheel. Building it from source links
# against the runner's Homebrew OpenSSL and produces a non-portable .app.
# 46.0.7 is the final tested universal2 wheel and embeds its own OpenSSL.
cryptography==46.0.7; sys_platform == 'darwin'
keyring>=25.7,<26; sys_platform == 'darwin'
pyobjc-framework-Cocoa>=11,<13; sys_platform == 'darwin'
pyobjc-framework-Quartz>=11,<13; sys_platform == 'darwin'
pyobjc-framework-AVFoundation>=11,<13; sys_platform == 'darwin'
